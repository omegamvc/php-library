<?php

declare(strict_types=1);

use Omega\Database\MySchema\Table\Create;
use Omega\Support\Facades\Schema;

return [
    'up' => [
        Schema::table('__table__', static function (Create $column) {
            // $column('id')->varchar(3);

            // $column->primaryKey('id');
        }),
    ],
    'down' => [
        Schema::drop()->table('__table__'),
    ],
];
